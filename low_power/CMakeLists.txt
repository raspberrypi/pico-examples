add_subdirectory_exclude_platforms(low_power_sleep host)
add_subdirectory_exclude_platforms(low_power_dormant host)
if (NOT PICO_RP2040)
    add_subdirectory_exclude_platforms(low_power_pstate host)
endif()
